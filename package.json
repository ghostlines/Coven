{
  "name": "Starfleet",
  "version": "0.1.0",
  "main": "main.js",
  "devDependencies": {
    "electron": "^1.6.2",
    "electron-packager": "^8.5.2"
  },
  "scripts": {
    "build": "electron-packager . --platform=darwin --arch=x64 --overwrite --icon=assets/icons/mac/appIcon.icns --extend-info=resources/mac/info.plist"
  }
}
